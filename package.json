{
  "name": "demo-app",
  "version": "1.0.0",
  "description": "IDP PoC demo app — Node.js HTTP server deployed via k8s-deployment spec",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
